fastapi==0.115.0
uvicorn[standard]==0.32.0
openpyxl==3.1.5
python-multipart==0.0.12
python-dotenv==1.0.1
httpx[socks]==0.27.2
requests==2.33.1
openai==1.54.0
anthropic>=0.40.0
rapidfuzz==3.10.0
pydantic==2.9.2
# Azure SQL (live SellerCloud catalog view) — pure-Python, no ODBC driver needed
python-tds>=1.15
pyopenssl>=24.0
certifi>=2024.2.2
azure-identity>=1.17
# Vendor Offer Analytics (Price Desk) — vendor catalog parsers
pandas>=2.0
xlrd>=2.0          # BILO's .xls (BIFF8) files
# services/health_check.py uses zoneinfo.ZoneInfo("America/New_York") at import
# time for its weekly scheduler. Windows (and some minimal Linux images) ship no
# system IANA tz database, so zoneinfo needs this PyPI package as its data
# source or it raises "No time zone found with key America/New_York" on import,
# crashing the whole app at startup. It only worked without this locally by
# accident (pulled in transitively by another package) -- declare it directly.
tzdata>=2024.1
